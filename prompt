general rules: i need no long descriptions; i need a summary for copy/paste the commit message; run if needed commands directly so that i can pass them by; read files completely; check scripts if the syntax is correct