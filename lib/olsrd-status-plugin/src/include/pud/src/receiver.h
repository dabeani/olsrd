/* Forwarding stub for pud/src/receiver.h kept inside the plugin. This simply
 * includes the authoritative receiver header under lib/pud/src when available
 * so core headers that expect "pud/src/receiver.h" can compile against the
 * plugin-local include path during plugin-only builds.
 */

#ifndef _PUD_SRC_RECEIVER_H_STUB
#define _PUD_SRC_RECEIVER_H_STUB

#include "../../../lib/pud/src/receiver.h"

#endif /* _PUD_SRC_RECEIVER_H_STUB */
